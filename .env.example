# Base URL of the King Domain backend.
VITE_API_BASE=http://localhost:4000

# Optional — Sentry error tracking for the admin frontend. Without it, errors
# are still shown in the UI but never reported anywhere (nothing captures a
# device-specific failure like a Safari-only cookie/CORS issue).
VITE_SENTRY_DSN=
